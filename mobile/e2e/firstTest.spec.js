const { reloadApp } = require('detox-expo-helpers');

describe('Test', () => {
    beforeEach(async () => {
        await reloadApp();
    });

    it('Show login buttons', async () => {
        // TODO: test
    });
});
